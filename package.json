{
  "dependencies": {
    "@codemirror/lang-sql": "^6.6.0",
    "@codemirror/state": "^6.4.1",
    "codemirror": "^6.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  },
  "scripts": {
    "build": "esbuild --bundle --minify --outfile=dist/editor.js editor.mjs"
  }
}
